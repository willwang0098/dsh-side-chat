//#region src/index.ts
const name = "@dsh-external/dsh-side-chat";
/** The plugin is client-only; this host entry lets DSH mount its client bundle. */
function apply() {}
//#endregion
export { apply, name };
